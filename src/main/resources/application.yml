spring:
  thymeleaf:
    mode: HTML
  profiles:
    active: dev
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/book?useUnicode=true&characterEncoding=utf-8&autoReconnect=true&useSSL=false
    username: root
    password: 111111
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
#logging:
#  config: classpath:logback-custom.xml
